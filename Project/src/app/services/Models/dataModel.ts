export class dataModel {
  //This model stores all the variables and their types that are expected to return from the server side
  valid:boolean;
  group = [];
  channel = [];
  role = [];
  user = [];
  message: string;
}
